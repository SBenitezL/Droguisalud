{% extends 'base.html.twig' %}

{% block title %}Producto{% endblock %}

{% block body %}
    <h1>Producto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producto.id }}</td>
            </tr>
            <tr>
                <th>Prd_nombre</th>
                <td>{{ producto.prdNombre }}</td>
            </tr>
            <tr>
                <th>Prd_unidad</th>
                <td>{{ producto.prdUnidad }}</td>
            </tr>
            <tr>
                <th>Prd_precio</th>
                <td>{{ producto.prdPrecio }}</td>
            </tr>
            <tr>
                <th>Prd_precio_unitario</th>
                <td>{{ producto.prdPrecioUnitario }}</td>
            </tr>
            <tr>
                <th>Prd_costo</th>
                <td>{{ producto.prdCosto }}</td>
            </tr>
            <tr>
                <th>Prd_iva</th>
                <td>{{ producto.prdIva }}</td>
            </tr>
            <tr>
                <th>Prd_cantidad</th>
                <td>{{ producto.prdCantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_producto_index') }}">back to list</a>

    <a href="{{ path('app_producto_edit', {'id': producto.id}) }}">edit</a>

    {{ include('producto/_delete_form.html.twig') }}
{% endblock %}
